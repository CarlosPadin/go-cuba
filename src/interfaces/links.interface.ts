import { ReactElement } from "react";

export interface navbarOptions {
  link: string,
  name: string,
  icon: ReactElement,
}

export interface socialLink {
  href: string,
  src: string,
  alt: string,
}